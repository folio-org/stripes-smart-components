/**
 * ExpandFilterPaneButton interactor
 */

import {
  interactor,
} from '@bigtest/interactor';

export default interactor(class ExpandFilterPaneButtonInteractor {
  static defaultScope = '[data-test-expand-filter-pane-button]';
});
